const caixaAlta = (frase) => frase.toUpperCase()
const primeiraLetra = (primeira) => primeira[0]
const concatenar = (st, se) => st.concat(se)

module.exports = { caixaAlta, primeiraLetra, concatenar }
